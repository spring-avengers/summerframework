spring:
  application:
    name: platform-dts-ops
    group: platform
    version: 1.0.0
eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: ${EUREKA_REGISTRY:http://localhost:8761/eureka/}